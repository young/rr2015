/**
 * @description Use Symbols to quickly iterate over an object
 *
 * Symbols have a unqiue property wherein they are skipped over when using a
 * for-of loop. If we use a symbol an object key, it will be skipped over 
 */

(function() {

})();